name: test_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  get: ^4.7.2
  http: ^1.3.0
  image_picker: ^1.1.2
  sqflite: ^2.0.1

  geocoding: ^2.1.0
  location: ^7.0.1
  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

assets:
  - asset/images/